import { FastifyInstance } from 'fastify'

export async function gymRoutes(app: FastifyInstance) {
  // app.post('/gyms', userRegisterController)
  // app.post('/gyms', userAuthenticateController)
  /* Authentication */
}
